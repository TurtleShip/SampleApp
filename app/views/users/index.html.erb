<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <!--The below iterates on each user and renders _user.html.erb-->
  <%= render @users %>
</ul>

<%= will_paginate %>